{
  "framework": "vite",
  "installCommand": "cd Front-End && npm install --include=dev",
  "buildCommand": "cd Front-End && npm run build",
  "outputDirectory": "Front-End/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
