{
  "buildCommand": "cd Front-End && pnpm install && pnpm build",
  "outputDirectory": "Front-End/dist",
  "framework": "vite",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
